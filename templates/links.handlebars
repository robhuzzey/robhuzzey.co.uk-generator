{{> header }}

	<h3>Some links I find interesting</h3>

	<ul>
		{{#links}}
			<li>
				<h4><a href="{{u}}" title="{{d}}">{{d}}</a></h4>
				<ul class="tags">{{#t}}<li>{{this}}</li>{{/t}}</ul>
			</li>
		{{/links}}
	</ul>

{{> footer }}